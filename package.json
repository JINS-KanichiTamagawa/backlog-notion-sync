{
  "name": "backlog-notion-sync",
  "version": "1.0.0",
  "description": "BacklogドキュメントをNotionに自動同期するツール",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "sync": "npm run build && npm run start"
  },
  "keywords": [
    "backlog",
    "notion",
    "sync"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}

